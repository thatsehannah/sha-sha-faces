import React from 'react';
import { Sidebar, SidebarContent } from '../ui/sidebar';

const AdminSidebar = () => {
  return (
    <Sidebar>
      <SidebarContent />
    </Sidebar>
  );
};

export default AdminSidebar;
