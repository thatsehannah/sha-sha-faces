import React from 'react';

const SectionBreaker = () => {
  return (
    <div className='w-full h-[35rem] bg-center bg-cover xl:bg-fixed bg-scroll bg-no-repeat bg-section-breaker' />
  );
};

export default SectionBreaker;
